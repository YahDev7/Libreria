
'use strict'
class CampaignBase{
     /**
     * Valida los datos dinamicos usados en la campaña con la cabecera de la base seleccionada
     * @param {Array} headers
     * @param {Array} dynamicData
     * @returns {String}
    */
    validateDynamicData(headers,dynamicData){
        //MSM y SHORT
    }
}